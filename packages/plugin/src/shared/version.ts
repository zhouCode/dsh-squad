/** Kept in lockstep with the root and plugin package manifests. */
export const SQUAD_VERSION = "0.7.3";
/** Primary build and acceptance target; runtime checks detect the installed peer. */
export const DSH_VERSION = "0.1.1-rc.2";
